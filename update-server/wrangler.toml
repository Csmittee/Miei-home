name = "mieihome-update-server"
main = "worker.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# ── D1 Database ───────────────────────────────────────────────────────────────
[[d1_databases]]
binding = "DB"
database_name = "mieihome-db"
database_id   = "7999c2c1-5364-43e1-bf84-2bf22b51aa7c"     # from: wrangler d1 create mieihome-db

# ── R2 Bucket (firmware binary storage) ──────────────────────────────────────
[[r2_buckets]]
binding    = "FIRMWARE"
bucket_name = "mieihome-firmware"

# ── Environment variables ─────────────────────────────────────────────────────
[vars]
R2_PUBLIC_URL = "https://firmware.mieihome.io"  # your R2 public domain

# ── Secrets (set via CLI — never in this file) ────────────────────────────────
# wrangler secret put ADMIN_TOKEN
# → paste a strong random token, keep it safe — used to auth admin endpoints

# ── Routes ────────────────────────────────────────────────────────────────────
routes = [
  { pattern = "updates.mieihome.io/*", zone_name = "mieihome.io" }
]

# ── Production environment ────────────────────────────────────────────────────
[env.production]
name = "mieihome-update-server-prod"
routes = [
  { pattern = "updates.mieihome.io/*", zone_name = "mieihome.io" }
]

# ── Staging environment (for testing before pushing to customers) ─────────────
[env.staging]
name = "mieihome-update-server-staging"
routes = [
  { pattern = "updates-staging.mieihome.io/*", zone_name = "mieihome.io" }
]
